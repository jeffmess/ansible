- hosts: localhost
  become: true
  pre_tasks:
    - name: Update cache
      apt:
        update_cache: true
      tags:
      - node
      - zsh
  tasks:
  - include: default-tasks/zsh.yml
  - include: default-tasks/node.yml

